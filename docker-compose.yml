services:
  resource-hub:
    build: .
    ports:
      - "3201:3201"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3201
      - API_KEY=your-secret-password-here
      - DB_PATH=/app/data/resource-hub.sqlite
      - AICARD_API_KEY=ak_live_1bjad0qZmUrnsEmXY0csIL9HWdTu9qkC
      - AICARD_CUSTOMER_ID=cus_e3cb4482c3d7139e41819997
    restart: unless-stopped
